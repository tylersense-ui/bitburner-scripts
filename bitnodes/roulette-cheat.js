/** @param {NS} ns **/
export async function main(ns) {
    ns.tail();
    ns.tprint("Astuce casino:");
    ns.tprint("Roulette seed déterministe.");
    ns.tprint("Sauvegarde → reload → mêmes résultats.");
    ns.tprint("Parie toujours noir/rouge après test.");
}
